import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LLM Speed Calculator — Estimate Token Generation Speed",
  description:
    "Calculate estimated token generation speed for any GPU + LLM model combination. Compare hardware, check VRAM requirements, and analyze costs.",
  openGraph: {
    title: "LLM Speed Calculator",
    description:
      "Estimate token generation speed for any GPU + LLM combination. Compare hardware, VRAM, and costs.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen`}
      >
        {children}
      </body>
    </html>
  );
}
